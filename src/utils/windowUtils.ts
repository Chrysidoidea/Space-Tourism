//just make media queries nice to read
const size = {
    tablet: "768px",
    desktop: "1440px",
};

export const device = {
    tablet: `(min-width: ${size.tablet})`,
    desktop: `(min-width: ${size.desktop})`,
};